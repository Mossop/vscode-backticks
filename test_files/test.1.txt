let tring = null;
let foo = "Double quoted $tring}"

foo = 'Single quoted $tring}'

foo = `Template literal $tring}`

console.log(foo);
console.log(tring);

// -----------------------------------------------------------------------------
let tring = null;
let foo = `Double quoted ${tring}`

foo = `Single quoted ${tring}`

foo = `Template literal ${tring}`

console.log(foo);
console.log(tring);

// -----------------------------------------------------------------------------
let tring = null;
let foo = `Double quoted ${}tring}`

foo = `Single quoted ${}tring}`

foo = `Template literal ${}tring}`

console.log(foo);
console.log(tring);
