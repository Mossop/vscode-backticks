"This is a string containing an end $quote" that we're going to replace"

// -----------------------------------------------------------------------------
"This is a string containing an end ${that we're going to replace"

// -----------------------------------------------------------------------------
"This is a string containing an end ${quote" }that we're going to replace"
